package com.springboot.singleton.utils;

import org.springframework.context.annotation.Description;

@Description(value = "Potential logging levels.")
public enum LogLevel {

    INFO, ERROR, WARN

}
